To setup:
 npm i
 sudo npm install -g browserify

To run tests:
 node test

To build client folder locally (publishes public):
 node build

To deploy static hosting to AWS
 node deploy

Production
 https://without-ceasing-static.s3.us-east-1.amazonaws.com/index.html